"use strict"; //treat all js codes a s newer version through engine
//alert("Hello")
//alert(3+3) //we are using node.js not browser, so availbale but not used

/*; is independent
code readibility is very important */

console.log(3+
    3
)//not a good practice

//tc-39 and mdn are the 2 most important documentation

//not so many datatypes
let name="Anuska" 
let age=20
let isLoggedIn= true

/*
number=> 2^53 is the range
bigInt
String=> " "
boolean=> true/false
null->empty value representation, can be assigned //standalone value
undefined=> variable declared, value not assigned
symbol=> (more exampls in react) => uniqueness
object(very imp)*/
console.log(typeof null)//output-> object, since null is an object
console.log(typeof undefined);
