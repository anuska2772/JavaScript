/**GArbage collector
 * the memory that is no longer in use in the program
 *  can be freed through garbage collector->
 * MEMORY MANAGEMENT
 * we have no control over the garbage collector,
 * runs in the bg always, no memory leaks 
 * 
 */